# 菜单栏应用验收

日期：2026-09-23，北京时间。本机环境见只读记录。

## 离线与打包

- 主控运行 `swift run EncoCoreChecks`：255 passed、0 failed。覆盖分包/粘包/坏长度、真实协议回包、事务响应匹配、空 ACK、EQ 曲线校验、重复 EQ id、恢复日志保护等。
- `./build.sh` Release 构建成功；两只 App 均通过 ad-hoc 签名及 `codesign --verify --deep --strict`。许可证和来源清单在 App Resources 内。
- `./scripts/install.sh` 安装到当前用户 `~/Applications`，安装后再验证签名。
- 本机无 XCTest，不将 executable checks 报告为 `swift test` 成功。

## 实机 UI

13:21–13:24，通过已安装的 App 进行原生辅助功能树与截图验收：

- 启动自动展开菜单栏面板；无水平裁切，电量、降噪、音效和设备列表均可见。
- 左右耳显示 100%，当前盒未上报，正确显示 `--` 和开盖入盒提示；盒电量成功证据见只读记录。
- 两个不同的已连接设备分别显示，修复重复行 ID 导致两行显示相同设备的问题。设备私人名称不写入本记录。
- 七种降噪控制、六个 EQ 选项和三个空间选项可用；日常界面没有 MAC/通道/命令码。
- 点击“通透”：写 256，ACK status=0，第一次查询精确回读 256；点击“关闭”恢复 8，同样精确回读。
- 通过 EQ 菜单选择“丹拿特调”：写 7，ACK + 回读 7；随后选择“自定义”恢复 4，ACK + 回读 4。
- 最终界面再次显示：降噪关闭、EQ 自定义、空间关闭。空间三个值的写控制已由 CLI 完成完整实机轮转，UI 此处只检查菜单，不重复改变空间设置。
- 修复一次刷新间隔问题：30 秒阈值配合 10 秒定时器可能跨过 30 秒新鲜度门限，导致音效菜单短暂禁用；改为 15 秒阈值，通常每 20 秒读取高级状态。电量/降噪维持 10 秒。

原始本地记录：`evidence/local/final-checks.txt`、`final-build.txt`、`final-install.txt`、`menu-final-errors.txt`（按钮成功与恢复）、`menu-delivery-errors.txt`（最终包启动与定期读取）。不将包含设备名称的 UI 截图提交入库。

## 尚未证明的部分

- 未做数天长期运行、睡眠反复、连接抢占压力测试。重连/唤醒/旧值清理代码已实现，不等于这些场景都已实测。
- 未做听感/声学测量，也未实现任何 Apple 专属生态能力。
- 未做 Developer ID 签名、公证或跨系统/跨固件兼容性验收。
