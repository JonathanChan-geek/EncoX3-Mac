# X3 模式切换实机验收

2026-09-23 13:04（北京时间）。采用固定源码快照与审查后的系统连接状态回退，签名应用经 IOBluetooth RFCOMM 运行 `cycle-anc`。

首次父模式测试见 `anc-first-cycle.md`。12:58 的第二次尝试因 IOBluetooth 状态标志误报，在任何 SET 前退出；系统连接信息仍有同一设备，增加按完整地址核对 `device_connected` 的只读回退后再次测试。

| 模式名称（型号表解释） | 发送位图 | 回读位图 | ACK |
|---|---|---|---|
| 关闭 | `0x0008` | `0x0008` | 0 |
| 固定通透 | `0x0100` | `0x0100` | 0 |
| 智能降噪 | `0x0080` | `0x0080` | 0 |
| 深度降噪 | `0x0010` | `0x0010` | 0 |
| 中度降噪 | `0x0020` | `0x0020` | 0 |
| 轻度降噪 | `0x0040` | `0x0040` | 0 |
| 自适应通透 | `0x0200` | `0x0200` | 0 |

所有请求/响应按 command 和 sequence 关联，7 项均逐项精确回读通过。最后发送原始位图 `0x0008`，ACK=0，回读 `0x0008`，`restoreVerified=true`。

这些证据验证协议写入和状态读回；模式名称依 X3 型号表，未用声学仪器或人工逐项听感判定降噪效果。未修改配对、固件、EQ 曲线或其它设置。

原始日志：`evidence/local/cycle-anc-03.txt`；恢复日志：`evidence/local/anc-restore-03.json`。未恢复的记录不得覆盖。
