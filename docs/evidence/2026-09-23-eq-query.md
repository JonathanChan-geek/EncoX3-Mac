# EQ 只读实机查询

2026-09-23 12:50（北京时间），以签名应用包经 IOBluetooth RFCOMM 查询。只发送 capability 和状态查询，无 SET。

请求 `0x0122`，payload `01 05`，sequence `F8`。响应 `0x8122` 同 sequence，payload 35 字节：

```
00 01 01 FA 06 04 09 E8 87 AA E5 AE 9A E4 B9 89
06 3E 00 FD FA 00 01 E8 03 01 A0 0F FE 40 1F 02 80 3E 02
```

按上游 EqInfo 格式解释，完整消费 35 字节：status=0，count=1，当前选中，ID=4，设备返回名称“自定义”，增益范围 -6…+6。

| 频率 Hz | 62 | 250 | 1000 | 4000 | 8000 | 16000 |
|---|---|---|---|---|---|---|
| 增益 dB | -3 | +1 | +1 | -2 | +2 | +2 |

同轮 `0x810F` payload `00 04`，与该条目一致。此次只返回一条自定义曲线，未返回内置预设全表；不能称“设备列出了所有预设”。未修改曲线或切换 EQ。

原始证据在 `evidence/local/eq-probe.txt` 和 `eq-probe-eqall.json`。探针结束时 pending writes=0，控制通道已关闭，无残留诊断进程。独立 Kimi 工单虽然完成查询，但报告状态标记被反引号包裹，dispatch 机器判定失败；本结论来自主控核对原始日志和 JSON，未以工单状态替代验收。
