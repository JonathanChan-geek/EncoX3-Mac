# Enco X3 for Mac v0.2.3

替换辨识度不足的菜单栏图标：改用 macOS 原生入耳式耳机矢量符号，保留耳塞、音腔及短柄细节，按比例使用 24 × 18 pt，自动适配深浅色菜单栏。移除旧的位图模板资源。

这是图标修正版，保留 v0.2.2 的异步连接和耳机控制实现。

已检查浅色、深色背景下的原尺寸及放大图标；Release 构建、安装版签名、DMG 校验及 ZIP 完整性检查通过。安装后实机重新连接并读回状态，本次没有发送耳机设置命令。

退出旧版，将 DMG 中的 App 拖入 Applications 并替换。Apple Silicon / arm64，最低部署目标 macOS 13；ad-hoc 签名，未做 Developer ID 签名及 Apple 公证。安装说明及 SHA-256 文件随包提供。

GPL-3.0-or-later；对应源码为 `v0.2.3` 标签。菜单栏符号由 macOS 系统提供，不代表 Apple 官方产品或生态功能支持。
